# Values for: gha-runner-scale-set-controller
# Chart: oci://ghcr.io/actions/actions-runner-controller-charts/gha-runner-scale-set-controller

replicaCount: 1

# Uncomment to restrict which namespaces the controller watches.
# By default it watches all namespaces.
# watchSingleNamespace: "arc-runners"

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi
